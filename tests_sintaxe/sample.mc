char *buffer[20];
int a, b;
int *d;
void **f;

void sum(int a[20]);
int mean(int a, int b);

int main(int argc, char **argv) {
    int g, h;

    g = a;
    h = a;

    if(a == 0) {
        puts(itoa(b, buffer));
    } else {
        for( ; b > 0; )
            if (a > b)
                a = a-b;
            else
                b = b-a;
        puts(itoa(a, buffer));
    }
    return 0;

}
